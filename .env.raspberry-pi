# Database Configuration for Raspberry Pi
# Since the database is running locally on the same Pi
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=kushkalsilav
DB_NAME=test

# LLaMA Server Configuration
# You don't have llama-server on Pi, so we'll skip this
LLAMA_SERVER_PORT=8080
FLASK_PORT=5000

# Security
SECRET_KEY=raspberry-pi-secret-key-change-this
API_KEY=
